export enum RoutersPaths {
  MAIN = '/',
  ADMIN = '/admin',
  USERBYID = '/admin/:id',
  AUTH = '/login',
  NOFOUND = '*'
}

export enum Status {
  LOADING = 'loading',
  SUCCES = 'succes',
  ERROR = 'error'
}
